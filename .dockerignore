# Docker 构建忽略：避免把宿主机上的打包产物/本地配置带进镜像。
#
# 特别重要：deploy/Dockerfile 有 `COPY dist dist`，那是给（历史上存在、
# 现已移除的）Web 前端预留的。若本地刚跑过 Windows 打包，dist/ 里会是
# "Publish Helper.exe"，会被一并 COPY 进镜像，既撑大体积又污染构建上下文。

# 打包产物与本地构建缓存
build/
dist/
release/
*.spec
!packaging/*.spec

# 本地环境与配置（真正的密钥不该进镜像）
.env
.venv/
venv/
__pycache__/
*.py[cod]

# 用户数据：compose 用 volume 挂载，不烘焙进镜像
media/
temp/
logs/

# 版本控制与文档噪声
.git/
.github/
docs/

# 编辑器
.idea/
.vscode/
